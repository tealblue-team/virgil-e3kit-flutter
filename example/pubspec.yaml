name: virgil_e3kit_example
description: Demonstrates how to use the virgil_e3kit plugin.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  virgil_e3kit:
    path: ../
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true